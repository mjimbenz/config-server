spring:
  application:
    name: config-server

  spring:
    cloud:
      config:
        server:
          git:
            uri: https://github.com/mjimbenz/config-repo
            default-label: main
            clone-on-start: true

